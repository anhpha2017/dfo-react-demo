import React from "react";
import "./Header.css";
const Header = React.memo(() => (
    <header>
        <h1>
            Todo App
          </h1>
    </header>
));

export default Header;
